import { View, StyleSheet, Text } from 'react-native';

export default function MainPage() {
  return (
    <View>
      <Text style={styles.container}>Cringe</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    color: "#fff"
  },
});
